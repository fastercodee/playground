{
  "name": "preview",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 9889",
    "build": "rm -f public/src_sw.ts.js && vite build && vite build -c vite-com.config.ts --emptyOutDir false && vite build -c vite-inject.config.ts --emptyOutDir false",
    "preview": "vite preview"
  },
  "devDependencies": {
    "esbuild": "^0.17.12",
    "rollup": "^3.19.1",
    "typescript": "^4.9.3",
    "vite": "^4.1.0",
    "vite-plugin-singlefile": "^0.13.3"
  },
  "dependencies": {
    "@fcanvas/communicate": "1.1.0-rc.40",
    "path-cross": "^0.0.3",
    "vue-console-feed": "^0.0.28"
  }
}
